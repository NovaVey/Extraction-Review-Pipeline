# Progress Log — Extraction Review Pipeline

## Phase 0 — Scaffold

### Summary
Monorepo scaffold (`/api` Fastify + Drizzle + `/health`, `/web` Vite+React+Tailwind placeholder), infrastructure provisioned on Railway + Supabase, `.env.example`, and this file. No business logic yet — `extraction_schemas`/`batches`/`documents`/etc. tables and all ingest/extract/confidence/review/export/eval code are Phase 1+.

### Files created/touched
- Root: `package.json` (npm workspaces `api`+`web`), `tsconfig.base.json`, `.env.example`, `.env` (gitignored, not committed), `PROGRESS.md`.
- `/api`: `package.json`, `tsconfig.json`, `vitest.config.ts`, `drizzle.config.ts`, `src/{app,index}.ts`, `src/db/{client,schema}.ts` (+`migrations/.gitkeep`), `src/routes/health.ts`, `src/lib/{env,logger,storage,anthropic}.ts`, `src/{ingest,extract,confidence,review,export,eval}/.gitkeep`, `test/health.test.ts`.
- `/web`: Vite React-TS scaffold (`npm create vite@latest web -- --template react-ts`), Tailwind v4 wired via `@tailwindcss/vite`, default demo content stripped from `App.tsx`/`main.tsx`, `src/styles/index.css` (`@import "tailwindcss";`), `.gitkeep` placeholders for `src/pages`, `src/components/{DocViewer,ReviewPane}`.
- `/docs`, `/scripts`, `/samples`: `.gitkeep` only — real content is later phases (docs: Phase 9; scripts/samples: Phase 1).

### Infrastructure provisioned
- **Railway:** Postgres deployed via the official `postgres` template (image `postgres-ssl:18`) as service **`Postgres-ERP`** inside the existing **"Upwork Portfolio"** project (id `f5549c0c-4269-4f60-a1dd-f12e6543ffc8`, env `production` id `6be5b32b-e74e-4d9a-b027-bc406b715688`), service id `2865a13c-bc05-4d34-ab49-8599363478d7`. Deployment status `SUCCESS`.
  - *Note:* a standalone `Extraction-Review-Pipeline` Railway project was created first (per the original isolation plan), but the user asked mid-build to place the DB under "Upwork Portfolio" instead — the standalone project (and its now-orphaned Postgres service) was deleted, and `Postgres-ERP` was deployed directly into "Upwork Portfolio" as a **dedicated** service (not reusing the predecessor's existing `Postgres`/`Postgres-zrMR` services, to keep data isolated even though the Railway project is now shared).
- **Supabase:** bucket **`erp-documents`** created in the existing "Upwork Portfolio" Supabase project (ref `vujkdmnvegfvtvxpyebw`), `public: false`, 20MB file size limit, allowed types `application/pdf`/`image/png`/`image/jpeg`. Named `erp-documents` (not the spec's literal `documents`) because that project already has a `documents` bucket owned by the predecessor `Document-Data-Extractor` repo.

### Decisions
- Bucket renamed from the spec's literal `documents` to `erp-documents` — confirmed with the user (name collision with the predecessor's bucket in the same shared Supabase project).
- **Bucket creation went through `supabase_execute_sql` (direct SQL insert into `storage.buckets`), not the Management API** — `POST /v1/projects/{ref}/storage/buckets` 404'd (the tool's dedicated bucket-create path doesn't exist server-side); the SQL fallback anticipated in the plan was used instead and verified via a follow-up `GET`.
- **This sandbox's network egress policy blocks direct outbound access to `*.supabase.co` (HTTPS) and to Railway's public Postgres TCP proxy** (raw TCP) — confirmed directly, and confirmed again by actually booting the real server with real `.env` values: `GET /health` returned `503` with `db.error: "Connection terminated due to connection timeout"` and `storage.error: "HTTP 403: Host not in allowlist: vujkdmnvegfvtvxpyebw.supabase.co. Add this host to your network egress settings to allow access."` — an explicit egress-allowlist rejection, not a credentials or code problem. `api.anthropic.com`, by contrast, **is** reachable from this sandbox: once the real `ANTHROPIC_API_KEY` was added, `anthropic.ok` came back `true` with `model: "claude-sonnet-5"` for real. So 1 of 3 checks is now genuinely verified end-to-end; **db and storage still need a real `/health` hit from a machine with normal internet egress** (per rule 10, the Windows dev machine) to confirm.
- Local `DATABASE_URL` uses Railway's `DATABASE_PUBLIC_URL` value, not the private-network `DATABASE_URL` the template also exposes — the private one resolves through `RAILWAY_PRIVATE_DOMAIN`, unreachable from outside Railway's own network.
- `pg.Pool` configured with `ssl: { rejectUnauthorized: false }` — the `postgres-ssl` template image serves a self-signed certificate.
- No `@supabase/supabase-js` dependency added — `api/src/lib/storage.ts` uses Node 20's built-in `fetch` against the Storage REST API directly (a single GET is all Phase 0 needs). Revisit when Phase 2 wants richer upload helpers.
- Tailwind v4 (`@tailwindcss/vite`, CSS-first `@import "tailwindcss";`) for `/web`, not v3/postcss.
- `api/src/db/schema.ts` is intentionally `export {}` — real tables are Phase 1.
- `@anthropic-ai/sdk` bumped from the initially-planned `^0.32.0` to `^0.115.0` — `client.models.retrieve()` (used by `pingAnthropic`) doesn't exist on 0.32.x; this is a version fix within the already-approved dependency, not a new one.
- Added a 5s `connectionTimeoutMillis` on the `pg.Pool` and a 5s `AbortSignal.timeout` on the storage fetch — confirmed while testing locally (see below) that an unreachable DB/storage host would otherwise hang `/health` indefinitely instead of reporting `degraded` promptly.

### Open questions
- **`EXTRACTION_TEMPERATURE=0.8` cannot be sent as a literal `temperature` request parameter to `claude-sonnet-5`** — verified directly against Anthropic's current API docs: current-generation models reject any non-default `temperature`/`top_p`/`top_k` with an HTTP 400. Omitting the parameter still yields real sampling diversity (the implicit default), so the Phase 3 confidence-scoring design (§5.2, sample agreement) is salvageable, but the extraction runner must never forward `temperature` on `messages.create()`. **Resolve the exact approach (drop the parameter but keep recording the configured value on the `extractions` row for audit purposes? Something else?) before starting Phase 3.**
- Exact npm package versions are whatever `npm install` resolves within the ranges pinned in `package.json`; see the generated lockfile once installed.

### Checkpoint — ALL ITEMS CONFIRMED (2026-07-31)
- [x] Supabase bucket `erp-documents` confirmed reachable — live `GET /health` from the user's machine returned `storage: {"ok": true, "bucket": "erp-documents"}`.
- [x] Railway Postgres (`Postgres-ERP` in "Upwork Portfolio") confirmed reachable — the user ran `npm run db:migrate --workspace api` from their own (Windows) machine, which connected and applied the migration; independently re-verified twice: a direct `pg_tables` query (all 12 tables present) and live `GET /health` returning `db: {"ok": true, "latencyMs": 1435}`.
- [x] Anthropic API key confirmed working — live `GET /health` returns `anthropic: {"ok": true, "model": "claude-sonnet-5"}`. Credit + spend limit confirmed set in-console by the user (2026-07-30).
- Final confirming `/health` response (user's machine, 2026-07-31): `{"status":"ok","checks":{"db":{"ok":true,"latencyMs":1435},"storage":{"ok":true,"bucket":"erp-documents"},"anthropic":{"ok":true,"model":"claude-sonnet-5"}}}`.

## Phase 1 — Schema + synthetic corpus

### Summary
Full Drizzle schema for all 12 tables from the spec, a generated (but not yet applied — see below) migration, and a 60-document synthetic corpus with a ground-truth manifest for later gold-set seeding.

### Files created/touched
- `api/src/db/schema.ts` — all 12 tables: `extraction_schemas`, `batches`, `documents`, `pages`, `extractions`, `field_values`, `field_value_rows`, `corrections`, `review_sessions`, `exports`, `gold_set_items`, `accuracy_snapshots`.
- `api/src/db/migrations/0000_mushy_scourge.sql` — generated via `drizzle-kit generate` (schema-diff only; does not require a live DB connection).
- `scripts/fieldSpecs.ts` — field/column specs for `invoice`/`receipt`/`purchase_order`, mirroring the `extraction_schemas.fields` jsonb shape; shared between the generator now and real schema seeding later.
- `scripts/make-synthetic-docs.ts` — generates the 60-document corpus into `/samples` (run via `npm run gen:samples`).
- `/samples/*.pdf` (60 files) + `/samples/manifest.json` (ground-truth ledger) — generated output, committed.
- `README.md` — added a "Synthetic data" section per rule 7.

### Corpus composition (verified)
- 60 PDFs: 15 clean digital, 15 scanned/skewed, 15 multi-page line-item tables, 15 edge cases — 20 invoices / 20 receipts / 20 purchase orders (evenly distributed within each difficulty group).
- 10 flagged `inDevSubset: true` in the manifest, spanning all four difficulty groups (3 clean, 3 scanned, 2 multipage, 2 edge_case).
- Edge cases cycle through all four kinds named in the spec: handwriting-in-one-field (rendered as a rasterized italic snippet embedded as an image, not text), two-currencies (one line item printed in € while the header currency says USD), missing-required-field (a required field omitted entirely — `null` in the manifest), and near-duplicate-vendor-name (e.g. "Blackwood Supply Co." vs "Blackwood Supply Co, Inc.").
- **Verified with `pdfjs-dist`** (temporarily installed with `--no-save` for this check only — not a project dependency yet): a clean doc's text layer extracts all field values (468 chars); a scanned doc's text layer extracts **zero** characters (confirming it's genuinely image-only); the handwriting edge-case doc's text layer contains every field's label and value **except** the one rendered as an image. A multi-page doc's page count is verified as 2 via `pdf-lib`.

### Decisions
- **PDF-authoring library: `pdf-lib`** (root `dependencies`, alongside `@napi-rs/canvas` for the scanned/handwriting raster rendering) — approved by the user; `pdfjs-dist` (already in the approved stack) only reads/rasterizes existing PDFs, it can't author new ones.
- `scripts/*.ts` are not an npm workspace (per the repo layout, `/scripts` is a sibling of `/api`/`/web`, not a member) — their dependencies (`pdf-lib`, `@napi-rs/canvas`, `tsx`) live in the **root** `package.json` instead, resolved via Node's normal upward `node_modules` lookup.
- `drizzle-kit` bumped `^0.28.0` → `^0.31.0` — 0.28.1's config loader broke with `"require is not defined in ES module scope"` under `"type": "module"` + Node 22; the newer version loads cleanly. Same category of fix as the `@anthropic-ai/sdk` bump in Phase 0 (correcting an initial version pin within an already-approved dependency).
- **The migration has been applied to the live Railway Postgres** — done from the user's own machine (this sandbox still can't reach `sakura.proxy.rlwy.net`, confirmed in Phase 0). All 12 tables verified present via a direct `pg_tables` query (2026-07-31).
- **`api/drizzle.config.ts` env-loading hardened during this troubleshooting**: the user's first `db:migrate` attempt failed with `url: undefined` because no `.env` existed at the repo root at all (only `.env.example` — the first file-send attempt never got placed there). Fixed the config to try multiple candidate `.env` locations (`import.meta.url`-relative and `process.cwd()`-relative) instead of exactly one, and to throw a clear error naming every path it tried if `DATABASE_URL` still isn't found, instead of a bare `undefined`.
- Note: `drizzle-kit migrate` on this version (`^0.31.0`) prints no success banner on completion — silence after `Using 'pg' driver for database querying` is normal, not a failure. Verify success by querying the DB directly (as done here) rather than expecting console output.
- **Important gotcha discovered — secrets pasted from chat text can get silently corrupted.** To unblock the missing `.env`, the user was given a `cat > .env << 'EOF' ... EOF` heredoc containing the real secrets as inline chat text to paste into git-bash. This got `DATABASE_URL` working (`db.ok` went `true`), but `storage` and `anthropic` then failed with a Node `ByteString`/`fetch` header error citing a character with code `8226` — Unicode `•` (bullet). A diagnostic script scanning the `.env` for any character above code 255 showed the **entire tail** of both `SUPABASE_SERVICE_KEY` (after `eyJhbGci...`) and `ANTHROPIC_API_KEY` (after `sk-ant-a...`) had been replaced with bullet characters. Root cause: something in the chat rendering/copy pipeline appears to display-redact text that pattern-matches a secret (recognizable prefix + long token) when it's rendered as copyable markdown, so copy-pasting a secret out of chat text is **not reliable** — the Railway Postgres URL wasn't redacted (doesn't match a "secret" pattern), but the JWT and the Anthropic key were. **Fix: deliver secrets only via a real file attachment (the `SendUserFile` tool), never as inline chat/code-block text.** Re-sent `.env` as an attachment; the user found it in `~/Downloads` saved as bare `env` (the browser stripped the leading dot — a separate, secondary Windows/browser quirk), moved it into place with `mv`, and it verified clean. `GET /health` then returned all three checks `ok: true`.
- Kept the "handwriting" simulation to an italic serif font at a slight rotation rather than sourcing an actual cursive/handwriting font file — avoids adding a font asset (and its licensing considerations) for a difficulty tier that only needs to be "not machine-text," not visually convincing.
- Line item counts: 3–6 for clean/scanned/edge-case docs, 28–42 for the multipage group (chosen to reliably overflow one page at the current row height/margins — verified to produce exactly 2 pages on the sampled doc).

### Open questions (carried over / new)
- Temperature/sampling-params incompatibility with `claude-sonnet-5` (from Phase 0) — still unresolved, still a Phase 3 concern. This is the only open item left overall.

### Phase 0 + Phase 1 checkpoints: fully closed (2026-07-31)
Live `GET /health` from the user's own machine: `{"status":"ok","checks":{"db":{"ok":true,"latencyMs":1435},"storage":{"ok":true,"bucket":"erp-documents"},"anthropic":{"ok":true,"model":"claude-sonnet-5"}}}`. All 12 tables confirmed present in the live DB. Nothing outstanding before Phase 2 except the temperature/sampling-params design question above.

## Phase 2 — Ingest

### Summary
PDF ingest pipeline (page rasterization, text-layer extraction, OCR fallback, sha256 hash-dedupe, Supabase Storage upload, `documents`/`pages` row writes), the `POST /schemas`, `POST /batches`, `GET /batches/:id`, and `POST /batches/:id/documents` routes, and two operator scripts to seed the three doc-type schemas and bulk-ingest the full 60-doc corpus.

### Files created/touched
- `api/src/ingest/pageRender.ts` — rasterizes each PDF page to a PNG via `pdfjs-dist` + `@napi-rs/canvas` (`RENDER_SCALE = 2.0`, ~144 DPI equivalent).
- `api/src/ingest/textLayer.ts` — per-page embedded-text extraction via `pdfjs-dist`; `hasTextLayer` is true once a page has ≥5 extracted characters.
- `api/src/ingest/ocr.ts` — `tesseract.js` OCR fallback for pages without a usable text layer; shares one worker across a whole ingest run.
- `api/src/ingest/upload.ts` — `ingestDocument()`: sha256-hash the upload, short-circuit on an existing `(batchId, sha256)` row, else upload the original PDF + each page's PNG to Storage, insert `documents`/`pages` rows, and mark the document `processed` (or `failed` with `failureReason` if any step throws).
- `api/src/lib/storage.ts` — added `uploadObject()` (content-addressed `POST .../object/{bucket}/{path}` with `x-upsert: true`) alongside the existing Phase 0 `pingStorage()`.
- `api/src/lib/pgErrors.ts` — `isUniqueViolation` / `isForeignKeyViolation` helpers (matches node-postgres's SQLSTATE `.code`), used to turn constraint violations into clean 409/400 responses instead of raw 500s.
- `api/src/routes/schemas.ts` — `POST /schemas` (Zod-validated `FieldSpec[]` body, 409 on duplicate name+version).
- `api/src/routes/batches.ts` — `POST /batches` (400 on unknown `schemaId`), `GET /batches/:id` (batch + its documents, 404 if missing), `POST /batches/:id/documents` (multipart, one or more PDFs, per-file success/error results; only `application/pdf` accepted since page images are derived, never uploaded directly).
- `api/src/app.ts` — registered `@fastify/multipart` (20MB file size cap, matching the Storage bucket's own limit) and the two new route plugins.
- `scripts/seed-schemas.ts` — inserts one v1 `extraction_schemas` row per doc type from `fieldSpecs.ts`; safe to re-run (skips an existing name+version).
- `scripts/ingest-corpus.ts` — reads `samples/manifest.json`, creates (or reuses) one batch per doc type, ingests all 60 documents via `ingestDocument()`, carrying each document's `inDevSubset` flag over from the manifest verbatim rather than re-deciding it. Idempotent re-run (batches reused by name+schema, documents deduped by sha256 inside `ingestDocument`); prints a final ingested/deduped/failed summary and exits non-zero if anything failed.
- `api/test/fixtures.ts`, `api/test/ingest/{hash,textLayer,pageRender,ocr,upload}.test.ts` — see Testing below.
- `api/package.json` — added `@fastify/multipart`, `pdfjs-dist`, `tesseract.js`, `@tesseract.js-data/eng`; `@napi-rs/canvas` bumped `^0.1.65` → `^1.0.0`.
- `api/tsconfig.json` — added `"lib": ["ES2022"]`.
- `package.json` (root) — added `"type": "module"`; `typecheck` script now also runs `scripts/tsconfig.json`.
- `scripts/tsconfig.json` (new) — gives `/scripts` the same real `tsc --noEmit` coverage as `api`/`web`.
- `.gitignore` — added `*.traineddata`.

### Decisions
- **`@napi-rs/canvas` bumped `^0.1.65` → `^1.0.0`.** `pdfjs-dist` v6's renderer calls `ctx.fill(path)` with a `Path2D` argument; 0.1.x's Canvas 2D implementation doesn't support that overload (`"Value is none of these types String, Path"`). Re-verified against both new (`pageRender.ts`) and existing (Phase 1's `make-synthetic-docs.ts`) consumers after the bump — no regressions.
- **OCR trained-data: added `@tesseract.js-data/eng` as a pinned dependency, rather than tesseract.js's CDN default.** tesseract.js normally fetches English trained data from `cdn.jsdelivr.net` at runtime, which this sandbox's egress policy blocks (`403`, same class of restriction as Supabase/Railway in Phase 0). `@tesseract.js-data/eng` ships the identical file as a plain npm package (`registry.npmjs.org` — already reachable, same host `npm install` itself uses); pointing `createWorker`'s `langPath` at its local `node_modules` path removes the CDN dependency entirely, in this sandbox **and** in production (no more runtime reliance on jsdelivr's uptime). Confirmed via `AskUserQuestion` before adding — a new dependency outside the originally-approved stack list. Also set `cacheMethod: 'none'`: without it, tesseract.js additionally writes a *decompressed* copy of the trained-data file into the current working directory on first use, which briefly left a stray untracked `eng.traineddata` (~5MB) at the repo root during testing; since `langPath` already reads instantly from a local, permanent path, that cache added no value and is now suppressed at the source. The stray file itself was deleted (never committed) and `*.traineddata` added to `.gitignore` defensively.
- **`api/tsconfig.json` root cause fix, not a workaround: `"lib": ["ES2022"]`.** `uploadObject()`'s `fetch(url, { body: someBuffer })` failed to typecheck with confusing "Buffer not assignable to BodyInit... missing URLSearchParams properties" errors. Root cause: neither `api/tsconfig.json` nor the shared `tsconfig.base.json` set `lib` explicitly, so TypeScript's target-based default (`ES2022` → implicitly `DOM` + `DOM.Iterable` too) pulled in `lib.dom.d.ts`'s own `fetch`/`BodyInit` globals alongside `@types/node`'s, and the two competing declarations didn't structurally agree on typed-array bodies. `/api` is pure backend code with no browser surface, so scoping it to `lib: ["ES2022"]` (no DOM) removes the phantom second declaration entirely — `/web`'s own independent tsconfig (Vite's standard split config) still has DOM and is untouched.
- **Root `package.json` root cause fix: added `"type": "module"`.** Every script under `/scripts` uses `import.meta.url` (ESM-only), but the root `package.json` never declared `"type": "module"` — under `moduleResolution: NodeNext`, that makes TypeScript treat root-level `.ts` files as CommonJS-intended by default (`tsc` even flagged it explicitly once `/scripts` got real typecheck coverage: `import.meta not allowed in CommonJS output`). `tsx`'s runtime loader is permissive enough that this never surfaced as a runtime failure (Phase 1's `make-synthetic-docs.ts` ran fine despite it), but it's a real latent inconsistency, not just a new-script problem — fixed at the root rather than worked around per-file.
- **`scripts/tsconfig.json` added**, extending the shared base, `include: ["**/*.ts"]`, wired into the root `typecheck` script. Justified now (wasn't in Phase 1) because `seed-schemas.ts`/`ingest-corpus.ts` are the first root-level scripts to import across the `/scripts` ↔ `api/src` boundary — exactly the boundary the `"type": "module"` bug above was hiding in.
- **Storage upload stays on plain `fetch`, no `@supabase/supabase-js`** — continues the Phase 0 decision; `uploadObject()` is one more REST call in the same style as `pingStorage()`.
- **Storage path convention:** `batches/{batchId}/{sha256}/original.pdf` and `batches/{batchId}/{sha256}/pages/{pageNumber}.png` — content-addressed by sha256 *within* a batch (scoped per-batch, not globally, so one batch's later retention purge can't delete another batch's copy of the same bytes) and upserted (`x-upsert: true`), so retrying a partially-failed ingest is always safe.
- **`documents.hasTextLayer` = true only if *every* page has a text layer; `documents.ocrRequired` = true if *any* page doesn't.** Matches how this was already verified against the corpus in this session (`hasTextLayer` cross-checked 0 mismatches against `manifest.json`'s `difficultyGroup !== 'scanned'`, which is itself a whole-document label).
- **Known limitation, not fixed this phase:** text-layer detection is whole-page, not per-field. The "handwriting-in-one-field" edge-case docs (Phase 1) embed only *one* field as a rasterized image inside an otherwise normal text PDF — the page's other fields keep `hasTextLayer` comfortably above the 5-character threshold, so OCR fallback never triggers for that page, and that one field's value is only recoverable from the page image, not from `pages.textContent`. Extraction (Phase 3+) will need to reason from the rendered page image for cases like this regardless of the page-level flag; flagging here so it isn't mistaken for a bug later.
- **`/samples` corpus was regenerated once during this phase** (`npm run gen:samples`), while verifying the `@napi-rs/canvas` bump didn't break Phase 1's generator — same composition (60 docs, 20/20/20 by doc type, 15/15/15/15 by difficulty group, 10 in the dev subset), fresh `Math.random()`-fabricated field values throughout. The generator's own design treats this as expected/harmless (its header comment notes `Math.random()` is deliberately unseeded, "this is a one-shot local script"), and every Phase 2 test in this session runs against the current on-disk corpus successfully — committing the regenerated files rather than reverting to Phase 1's original random draw.
- **`POST /batches/:id/documents` only accepts `application/pdf`** — page images are always derived from the uploaded PDF by this pipeline, never uploaded directly, so accepting image mimetypes here would be misleading.
- **`scripts/ingest-corpus.ts` groups the corpus into one batch per doc type** (`"invoice corpus"`, `"receipt corpus"`, `"purchase_order corpus"`), matching the schema-per-batch relationship (`batches.schemaId` is a single FK) and the fact each doc type has its own schema from `seed-schemas.ts`.
- **Bug found and fixed on the user's Windows machine, not visible in this sandbox: `pageRender.ts`'s `GlobalWorkerOptions.workerSrc`.** pdfjs-dist loads it via a dynamic `import()`, which needs a real URL. A raw `path.resolve()` result happens to work on POSIX (no scheme ambiguity) but fails on Windows — a drive letter like `C:\Users\...` gets misparsed as a URL scheme (`"Setting up fake worker failed: ... Received protocol 'c:'."`), and every one of the 60 documents failed on the first `ingest-corpus.ts` run as a result. Fixed by wrapping the resolved path in `pathToFileURL(...).href` (`node:url`), which is correct on every platform — not a Windows-only branch. `standardFontDataUrl` (same file) turned out not to need the same fix: it's consumed via `fs.readFile()` internally (traced through pdfjs-dist's source to confirm rather than assume), which accepts raw OS paths fine. Re-ran the full test suite in this sandbox after the fix — no regressions — then the user re-ran `ingest-corpus.ts` on Windows: `ingested=60 deduped=0 failed=0`.

### Testing
- **Fully verified in-sandbox with real execution (no mocks, no network needed):** `pageRender.ts` and `textLayer.ts` against real sample PDFs (`api/test/ingest/{pageRender,textLayer}.test.ts`); `sha256Hex` against a digest cross-checked three independent ways (Node `crypto`, `sha256sum`, `openssl`) after an earlier hand-transcribed test literal was caught wrong by the test itself; **`ocr.ts` against a real scanned sample** (`api/test/ingest/ocr.test.ts`) — genuinely new capability this phase, since the `@tesseract.js-data/eng` fix means OCR no longer needs network access at all, in-sandbox or otherwise.
- **`upload.ts` orchestration** (`api/test/ingest/upload.test.ts`): DB (`db`) and `uploadObject` are mocked (Railway/Supabase are still unreachable from this sandbox, unchanged from Phase 0/1), but real sample PDFs flow through the real `pageRender`/`textLayer` code underneath. Covers the dedupe short-circuit, the full upload+insert+processed path, and the upload-failure → `status: 'failed'` + rethrow path.
- All 13 tests pass (`npm run test --workspace api`); full monorepo `npm run typecheck` (api + web + scripts) is clean.
- **Not verifiable in this sandbox — needs the user's own machine, same as Phase 0/1's DB migration:** `POST /schemas`, `POST /batches`, and `POST /batches/:id/documents` were exercised locally far enough to confirm Zod validation and routing are correct (invalid body → 400 with field errors; unknown route → 404), but every DB-touching path fails with the same `Connection terminated due to connection timeout` seen in Phase 0 — confirmed still true today by a direct fresh probe of both Railway Postgres and Supabase Storage (`HTTP 000`/`CONNECT tunnel failed, response 403`), not a regression. `scripts/seed-schemas.ts` and `scripts/ingest-corpus.ts` are written and typecheck clean but have never actually run against the live DB — that has to happen on the user's machine.

### Open questions (carried over)
- Temperature/sampling-params incompatibility with `claude-sonnet-5` (from Phase 0) — still the only unresolved item overall, still a Phase 3 concern.

### Checkpoint — ALL ITEMS CONFIRMED (2026-07-31)
- [x] `npx tsx scripts/seed-schemas.ts` (user's machine) created 3 `extraction_schemas` rows: `invoice` v1 (`1b7c9743-f03c-49bb-9980-514868d473c4`), `receipt` v1 (`bcfc4b1a-7c2e-48af-82cc-b22d061aa79e`), `purchase_order` v1 (`082283b6-9e51-4a50-8112-77b471ce07a7`).
- [x] `npx tsx scripts/ingest-corpus.ts` reported `ingested=60 deduped=0 failed=0` (after the Windows `pathToFileURL` fix above), across 3 batches: `invoice corpus` (`a2734476-...`), `receipt corpus` (`597e2ae8-...`), `purchase_order corpus` (`c5ed90cc-...`).
- [x] `GET /batches/a2734476-9d34-4a65-ba32-cfa51cd7e01d` spot-checked against `manifest.json`: 20 documents, all `status: "processed"`; all 5 `invoice_scanned_*` docs show `hasTextLayer: false, ocrRequired: true`; all others show `hasTextLayer: true, ocrRequired: false`; multipage docs show `pageCount: 2`; `storagePath` matches the `batches/{batchId}/{sha256}/original.pdf` convention exactly.
- [x] Storage bucket confirmed via a direct `storage.objects` query against the Supabase project's own Postgres (not just a dashboard glance): **60** `original.pdf` objects, **3** distinct batch folders, **132** objects total. Page-image count needed one extra check — 72 PNGs, not the naively-expected 75 (45 single-page docs + 15 multipage docs × 2) — resolved by grouping per batch: `invoice` and `receipt` each have all 5 multipage docs at 2 pages, but `purchase_order` only has 2 of its 5 at 2 pages (18 of its 20 docs are 1 page, not the expected 15). Not an ingest bug — the pipeline uploaded exactly as many page images as each PDF actually has; it's a Phase 1 corpus-generation characteristic (the 28–42 random line-item count for the "multipage" group was only ever verified to overflow to page 2 on one sampled document, not all 15, and purchase orders' slightly different field layout — no `tax` line — apparently gave 3 of the 5 just enough extra room to fit on one page at the low end of that range). Multi-page rendering itself is still solidly exercised: 12 real 2-page documents rendered and uploaded correctly.
- Full verification data: `originals: 60, page_images: 72, total: 132, distinct_batches: 3` (queried directly, not estimated).

## Phase 3 — Extract

### Summary
LLM-based structured extraction: one Anthropic Messages API call per sample (`SAMPLE_COUNT=3` samples per document), each given every page's extracted text *and* rendered image, constrained to a JSON Schema built from the document's `extraction_schemas.fields`. Samples are combined by self-consistency voting — the value most samples agree on is stored, and the agreement fraction becomes the field's confidence — writing `extractions`, `field_values`, and `field_value_rows` rows. Wired into `POST /documents/:id/extract` and `scripts/extract-devset.ts`. Hard-restricted to the 10-doc dev subset (`documents.inDevSubset`), enforced in code, not just by convention.

### Files created/touched
- `api/src/extract/schema.ts` — `buildExtractionJsonSchema(fields)`: maps `extraction_schemas.fields` (`FieldSpec[]`) to the JSON Schema passed as `output_config.format.schema`.
- `api/src/extract/anthropic.ts` — `extractSample()`: builds the per-page text+image content blocks, calls `client.messages.parse()` with `output_config.format` (structured output) and `thinking: {type: 'adaptive'}`, no `temperature`.
- `api/src/extract/run.ts` — `extractDocument(documentId)`: the orchestrator (DB reads, N parallel samples, majority-vote confidence, basic per-field validation, DB writes). Exports `pickMajority`/`validateField` directly for unit testing.
- `api/src/lib/storage.ts` — added `downloadObject()` (Phase 2 only had `uploadObject`/`pingStorage`) to fetch page PNGs back out of Storage for the extraction request.
- `api/src/routes/extract.ts` — `POST /documents/:id/extract`, registered in `app.ts`.
- `scripts/extract-devset.ts` — bulk-runs extraction over every `inDevSubset: true` document.
- `api/test/extract/{schema,run}.test.ts` — see Testing below.

### Decisions
- **Structured Outputs (`output_config.format`), not tool-use, for extraction.** `client.messages.parse()` validates/parses the response against the schema automatically (confirmed by reading the SDK's actual `parser.js`: for a plain `json_schema` format — not just a Zod-derived one — it falls back to `JSON.parse()` on the response text, so this works without a Zod dependency). This is the tool built for "extract into a schema," not a fit-for-purpose-but-wrong tool.
- **Nullability requires `anyOf`, not a `type` array — found via a real 400, not assumed.** The first live request with `currency: {type: ['string','null'], enum: [...]}` was rejected: `"Enum value 'USD' does not match declared type '['string', 'null']'"`. Fixed by expressing nullable fields as `{anyOf: [{type: 'string', enum: [...]}, {type: 'null'}]}` instead — every field is still marked `required` (must be present in the response), but its value can legitimately be `null` for a field genuinely absent from the document (the `missing_required_field` synthetic edge case is exactly this case, and is meant to produce a real, non-error null, not a fabricated value).
- **Every page is sent as both extracted text and a rendered image**, not one or the other. Text alone would fail outright on the 15 scanned docs and the handwriting edge case (no usable text layer, by design); images alone would waste tokens re-deriving what OCR/the text layer already got right on the other 45. The system prompt explicitly tells the model the image is ground truth when the two disagree.
- **The Phase 0 temperature question is resolved.** `claude-sonnet-5` still rejects a non-default `temperature` with a 400 (re-confirmed via the `claude-api` skill). `extractions.temperature` (`NOT NULL`) stores the *configured* `EXTRACTION_TEMPERATURE` value for audit purposes — it documents the intended sampling setting without ever being sent on the request. Real sample-to-sample diversity for the multi-sample design instead comes from adaptive thinking's implicit variance (on by default for `claude-sonnet-5`), which a live test confirmed still produces genuinely differing outputs across samples.
- **Confidence in Phase 3 is exactly one signal: sample agreement.** `field_values.confidenceParts` is deliberately an extensible jsonb bag (`{sampleAgreement: 0.67}` today); the `/confidence` directory scaffolded since Phase 0 is where later signals (validator results, cross-field consistency, historical accuracy) get composed into a calibrated score. `field_values.status` is set to `'pending'` for every field this phase — auto-accept/needs-review routing against each field's `autoAcceptThreshold` is a confidence/review-phase decision, not an extraction one.
- **`field_values.validatorStatus` (`NOT NULL`) is basic type-format checking, not business-rule validation.** Per field type: `date` must `Date.parse()`, `money` must be numeric after stripping symbols, `enum` must be one of the field's `enumValues`, everything else just non-empty; `null` → `'missing'`. Deliberately does *not* check cross-field consistency (e.g. subtotal+tax=total) — the two_currencies edge case exists specifically to exercise that kind of check later, in the confidence/review phases, not here.
- **Money values are stored two ways**, discovered from a real extraction, not assumed: the model transcribes amounts as literally printed (`"$106.81"`, matching a live test against `invoice_clean_01.pdf`), but `manifest.json`'s gold-set ground truth stores the stripped form (`"106.81"`). `rawValue` keeps what the model actually said; `normalizedValue` strips currency symbols/commas for `money` fields specifically, so it's comparable to gold-set values later. Other field types pass through unchanged for now.
- **`inDevSubset` is enforced inside `extractDocument()` itself**, not just in the script that happens to only query dev-subset rows — so the one guaranteed choke point both the route and the bulk script share can't be bypassed by calling the route directly on a non-dev-subset document. Matches the explicit project rule restricting extraction to 10 documents to control API cost.
- **`FieldSpec`/`ColumnSpec` types are declared locally in `api/src/extract/schema.ts`**, structurally identical to but not imported from `scripts/fieldSpecs.ts` — `/api` should stay deployable without `/scripts` existing at runtime, and jsonb columns come back untyped from Drizzle regardless of where the TS type is declared, so there's no real coupling benefit to importing across that boundary in this direction (unlike Phase 2's scripts → api imports, which go the natural "operator script drives the app's own code" direction).
- **`PROMPT_VERSION` (`'v1'`)** stored on every `extractions` row — bumped whenever the system prompt or schema-building logic changes in a way that would make old and new extractions non-comparable for eval/gold-set purposes later.
- **`GET /documents/:id/extraction` added** (`api/src/routes/documents.ts`) to actually read extraction results back — there was no way to inspect what got extracted otherwise. Returns the *most recent* extraction for a document (ordered by `startedAt DESC` — extraction isn't deduplicated like ingestion, so a document can have several `extractions` rows over time) plus its `field_values`, with `field_value_rows` nested under table-type fields. Deliberately excludes the full `rawResponses` blob from the response (could be large; exposes only `sampleResponseCount` as a cheap audit signal that all N samples were actually stored). Built via a small Workflow: one agent implemented it, then two independent agents reviewed it in parallel — a general code reviewer, and a second agent that re-derived the correct join/ordering logic from `schema.ts` from scratch *before* looking at the implementation, to catch a subtly-wrong query without being anchored by it. Both passed the implementation; the code reviewer's one real finding (an unguarded `as unknown[]` cast on the untyped `rawResponses` jsonb column, which would 500 on a malformed row instead of degrading gracefully) was fixed before commit.

### Testing
- **Real, live smoke test against the actual Anthropic API** (`api.anthropic.com` is reachable from this sandbox, unlike Railway/Supabase — confirmed since Phase 0): ran one real extraction against `invoice_clean_01.pdf`. Every field matched `manifest.json`'s ground truth exactly — `invoice_number`, `vendor_name`, both dates, `currency`, `subtotal`/`tax`/`total`, and all 6 line items (descriptions, quantities, unit prices, amounts) — with `stop_reason: "end_turn"` and no truncation (4098 input / 398 output tokens against an 8192 cap). This is the run that surfaced both the `anyOf`-vs-`type`-array issue and the money-symbol-stripping need, fixed before anything else was built on top. **Not part of the persisted test suite** — a real API call in `npm test` would spend real money on every future run; used once, manually, to validate the design, matching how DB/Storage live checks work in this project generally.
- **`schema.test.ts`** (4 tests, pure, no network): required-but-nullable-via-`anyOf` shape, enum-in-the-non-null-branch, table-field structure.
- **`run.test.ts`** (10 tests): `pickMajority`/`validateField` tested directly as pure functions; `extractDocument()` tested with DB, Storage, and `extractSample` all mocked (matching Phase 2's `upload.test.ts` pattern) — covers the `inDevSubset` guard short-circuiting before any model call, the full majority-vote-and-persist path (including a table field with real row aggregation), and the all-samples-failed path (writes a `status: 'failed'` extraction row, throws, and writes zero `field_values` rows).
- All 30 tests pass (`npm run test --workspace api`, including `documents.test.ts` for the new read route via `app.inject()`, matching `health.test.ts`'s route-testing convention); full monorepo `npm run typecheck` clean.

### Checkpoint — ALL ITEMS CONFIRMED (2026-08-01)
- [x] `npx tsx scripts/extract-devset.ts` (user's machine) reported `succeeded=10 failed=0 total=10` across all four difficulty groups (clean/scanned/multipage/edge_case) and all three doc types.
- [x] `GET /documents/66fdcaaf-a701-4afa-bafc-8b75f2dbfbb7/extraction` (`invoice_clean_01.pdf`) spot-checked against `manifest.json`: every field matches exactly — `invoice_number: "INV-17087"`, `vendor_name: "Harrow & Finch Materials"`, both dates, `currency: "USD"`, `subtotal`/`tax`/`total`, and all 6 line items (descriptions, quantities, unit prices, amounts) — with `confidence: "1"` on every field (all 3 samples agreed on all 9 fields for this clean document).
- [x] Audit fields confirmed populated: `sampleResponseCount: 3` (matches `sampleCount: 3` — all 3 raw API responses actually stored, not just claimed), `inputTokens: 12294`, `outputTokens: 1194`, `status: "completed"`, `startedAt`/`finishedAt` ~4.7s apart for 3 parallel samples.
- Not proceeding to Phase 4 until the user directs it, per the workflow rules.

## Phase 4 — Confidence Scoring

### Summary
Replaces Phase 3's placeholder confidence/status handling (`confidenceParts: {sampleAgreement}` only, `status: 'pending'` on every field regardless of `autoAcceptThreshold`) with a real scoring model: a new `api/src/confidence/` module combines sample agreement, per-field/per-cell format validation, and cross-field business-rule reconciliation (subtotal+tax=total, line-item sum vs subtotal, quantity×unit_price=amount) into a single confidence number, then decides `auto_accepted` vs `needs_review` against each field's configured `autoAcceptThreshold`. Applies to both `field_values` and, for the first time, individually to each `field_value_rows` line item — rows previously just inherited the parent table's agreement verbatim with no validation of their own cells.

### Files created/touched
- `api/src/confidence/validate.ts` — `validateValue(type, enumValues, rawValue)`: generalizes Phase 3's `validateField` to also validate individual line-item cell values (added a real `'number'` case, previously ungoverned); `stripMoneySymbols`/`parseMoney`.
- `api/src/confidence/score.ts` — `computeConfidence(input)`: the scoring formula (see Decisions); `decideStatus(confidence, autoAcceptThreshold)`.
- `api/src/confidence/crossFieldChecks.ts` — `computeCrossFieldChecks(scalars, lineItemRows)`: the deferred cross-field reconciliation logic, returning per-field and per-row check results.
- `api/src/extract/run.ts` — restructured from a single pass (compute-and-insert-immediately per field) to two passes: pass 1 computes every field's majority value + validator status without touching the DB; a cross-field pass reconciles totals and line-item math using every field's pass-1 result at once; pass 2 scores and inserts. `validateField` kept as a thin wrapper over `validateValue` (existing tests/signature unchanged); `normalizeValue` now calls `stripMoneySymbols` instead of duplicating the regex.
- `api/test/confidence/{validate,score,crossFieldChecks}.test.ts` (35 tests) — new, pure-function coverage.
- `api/test/extract/run.test.ts` — extended the shared fixture with `vendor_name`/`subtotal`/`tax`/`total` fields and full line-item columns (`quantity`/`unit_price`/`amount`, not just `description`); added tests for a required-missing field (confidence forced to 0), a reconciling vs. mismatched total (auto_accepted vs needs_review), and three new row-level tests (reconciling row → auto_accepted, arithmetic mismatch → needs_review at 0.5 confidence, invalid cell → confidence forced to 0).

### Decisions
- **Built via a Workflow: one agent implemented against a fully-specified formula (not left to invent one), two independent agents reviewed in parallel** — a general code reviewer, and a second agent whose only job was to hand-trace 7 concrete scenarios (reconciling/mismatched totals, required-vs-optional missing fields, invalid dates, row arithmetic, a no-tax doc type) through the actual code and compare against expected answers I specified up front. Both passed; findings below were folded in before commit.
- **The scoring formula** (`computeConfidence`): `validatorStatus: 'invalid'` → confidence is always `0`, unconditionally — no amount of sample agreement can rescue a value we already know is malformed (verified directly: even 3/3 samples agreeing on an unparseable date string still scores `0`). `validatorStatus: 'missing'` → `0` if the field is `required`, else `sampleAgreement` (unanimous agreement that an *optional* field is legitimately absent, e.g. an unsigned PO's `approved_by`, is real signal, not a defect). Otherwise → `sampleAgreement * 0.5 ^ (number of failed cross-field checks)` — each unreconciled cross-field check halves confidence rather than zeroing it, since a value can still be individually correct even when a related total doesn't add up (unlike a format failure, which is already known-bad).
- **Cross-field checks are "skip if not evaluable," never "fail by default."** If subtotal/tax/total aren't all individually `validatorStatus: 'valid'` first, no totals check is recorded at all for any of them — this avoids double-penalizing a value that's already being penalized for its own format failure. Same rule for the line-item sum and per-row arithmetic checks. Money comparisons use a $0.01 tolerance to absorb float rounding, not as a business "close enough."
- **Doc-type dispatch for the totals check (subtotal+tax=total vs subtotal=total) is inferred from data, not passed in.** Whether a `'tax'` key exists at all in the field-value map (invoice/receipt have one; `purchase_order` doesn't, per `scripts/fieldSpecs.ts`) already discriminates the two cases; an earlier draft threaded `docType` through as an explicit parameter, but nothing in the function actually branched on it once written — the general-review pass flagged it as dead API surface, and it was removed rather than kept as unused-but-documented.
- **Line items get real per-row validation for the first time.** Each row's own `validatorStatus` is now aggregated from validating every column's cell (`validateValue` per `column.type`) — any format-invalid cell taints the whole row (a line item can't be "mostly right"); a missing *optional* column doesn't. Combined with the new `quantity × unit_price = amount` per-row check, a line item with internally-inconsistent math or a garbage cell now actually routes to `needs_review` instead of silently inheriting whatever confidence the *other* rows in the same table happened to get.
- **A known, documented limitation:** every row in a table field still shares the *same* `sampleAgreement` value (the whole array is one unit for majority-voting purposes, per Phase 3's `pickMajority` — unchanged this phase). Per-row confidence now genuinely differs where a row's own validity or arithmetic differs, but agreement itself isn't yet computed per-row. Not fixed here — would require row-level (not whole-array) self-consistency voting, a larger change than this phase's scope.
- **A known, unreached edge case:** `decideStatus(confidence, autoAcceptThreshold)` is a plain `confidence >= threshold` comparison, so a hypothetical `autoAcceptThreshold: 0` would auto-accept even a confidence-`0` (known-invalid or missing-required) field. No field in `scripts/fieldSpecs.ts` is configured below `0.9`, so this doesn't manifest today; flagged by the conformance-review pass and left as-is rather than adding a guard against a configuration that doesn't exist, per the project's general rule against validating unreachable scenarios.

### Testing
- 71 tests pass (`npm run test --workspace api`, up from 30 in Phase 3): 35 new pure-function tests across the three `confidence/` modules, plus `run.test.ts` grown from 10 to 16 tests (6 new: required-missing, reconciling/mismatched totals, and the three new row-level scenarios). Full monorepo `npm run typecheck` clean.
- All test-level confidence numbers are hand-verified against the formula above, not just asserted to "look reasonable" — e.g. the mismatched-total test asserts exactly `confidence: '0.5'` (`sampleAgreement=1` halved once for one failing check) against a `0.9` `autoAcceptThreshold`, and the conformance-review agent independently re-derived and cross-checked the same numbers by calling the real functions directly (via a temporary scratch test file, written and deleted, not left in the repo) rather than trusting the implementation's own claims.
- **Not verifiable in this sandbox — needs a live extraction run on the user's machine, same as every DB-touching phase since Phase 0:** the formula is fully unit-tested against mocked data, but has not yet run against the real 10-doc dev subset. `field_values.status`/`field_value_rows.status` will now show real `auto_accepted`/`needs_review` values instead of uniform `'pending'`, and the money-total/line-item-math cross-field checks will run against real (synthetic) document data for the first time.

### Checkpoint — ALL ITEMS CONFIRMED (2026-08-01)
- [x] `npx tsx scripts/extract-devset.ts` (user's machine, after pulling this phase's commit and restarting the API server) reported success across all 10 dev-subset documents, creating fresh `extractions` rows scored under the Phase 4 logic.
- [x] `GET /documents/66fdcaaf-a701-4afa-bafc-8b75f2dbfbb7/extraction` (`invoice_clean_01.pdf`) spot-checked against `manifest.json`: every field and all 6 line items match exactly, same as the Phase 3 baseline — now additionally showing `status: "auto_accepted"` throughout (fields and rows individually, not just the field-level value from Phase 3) and populated `confidenceParts.crossFieldChecks` (`subtotal_plus_tax_equals_total` and `line_items_sum_equals_subtotal` both `passed: true`).
- [x] `GET /documents/05649135-70d4-45f1-9e41-1f9e656b8165/extraction` (`receipt_edge_case_02.pdf`, the deliberate-currency-inconsistency edge case) spot-checked against `manifest.json`: every field and all 4 line items match exactly, including row 3's amount/unit_price preserved verbatim as printed (`"€7.62"`, not silently converted or normalized) — confirms `field_value_rows.cells` still correctly skips normalization (Phase 3's documented scope boundary). All fields and rows came back `auto_accepted`.
- **Observed limitation, not a defect in what this phase committed to:** the currency-inconsistency edge case did not trip a review flag. `line_items_sum_equals_subtotal` does currency-agnostic arithmetic (`parseMoney` strips `€` and `$` identically), and the synthetic document's amounts sum correctly regardless of symbol — so nothing is numerically inconsistent. Receipts also have no `currency` field in `fieldSpecs.ts` to cross-check a row's symbol against. A human reviewer would notice the mixed `$`/`€` symbols on sight; nothing in the pipeline currently checks for that. Worth revisiting (a per-row currency-symbol-consistency check, or surfacing it in the review UI) in a later phase — not fixed here since it's new scope, not a bug in the formula this phase specified.
- Not proceeding to Phase 5 until the user directs it, per the workflow rules.

## Phase 5 — Review

### Summary
A human review queue, end to end: a backend API (queue query, accept/correct actions, review-session tracking, a page-image proxy) and the actual frontend UI — `web/src/App.tsx`, `DocViewer`, `ReviewPane` had been Phase 0 placeholders (`.gitkeep`s, and `App.tsx` literally read "review UI starts in Phase 5") until now. A reviewer works through one `needs_review` item at a time — either a scalar field or a table field with a flagged row — sees the document page image alongside the field's confidence/validator/cross-field-check context, and either accepts it as extracted or corrects it, with every resolution (including a plain accept) written to `corrections` for audit. Built and reviewed as two sequential passes (backend, then frontend against the verified backend contract), each via implement-then-independently-review.

### Files created/touched
- `api/src/review/queue.ts` — `getNextReviewItem(batchId?)`: the lowest-confidence outstanding candidate (a `needs_review` field, or a table field with a `needs_review` row underneath it even if the field's own status is `auto_accepted`), filtered to each document's *current* extraction only (see Decisions), assembled with page/column/label context for rendering.
- `api/src/review/actions.ts` — `acceptField`/`correctField`/`acceptRow`/`correctRow`, `startReviewSession`/`endReviewSession`.
- `api/src/routes/review.ts` — `POST /review-sessions[/:id/end]`, `GET /review/next`, `POST /review/fields/:id/{accept,correct}`, `POST /review/rows/:id/{accept,correct}`.
- `api/src/routes/pages.ts` — `GET /pages/:id/image`, proxying a page PNG out of private Storage.
- `api/test/review/{queue,actions}.test.ts`, `api/test/review.route.test.ts` (39 tests).
- `web/vite.config.ts` — dev-server proxy (`/api` → `localhost:3000`).
- `web/src/App.tsx` (full rewrite), `web/src/api.ts`, `web/src/types.ts`, `web/src/components/DocViewer/DocViewer.tsx`, `web/src/components/ReviewPane/{ReviewPane,RowTable,Badges}.tsx`.

### Decisions
- **Zero new dependencies, on either side.** No `@fastify/cors` — a Vite dev-server proxy makes every frontend request same-origin instead. No router, no data-fetching/state library, no hotkey library — this is one continuous view (name gate → review loop), and React's built-in hooks + native `fetch`/`keydown` are enough. Avoids re-litigating the "ask before adding dependencies outside the approved stack" rule entirely, since nothing needed to be asked.
- **The `corrections` table is the general-purpose human-review audit log, not just an edit log.** Every resolution — accept-as-is or an actual edit — writes a `corrections` row, with `oldValue`/`newValue` identical for a plain accept. This is *required*, not just tidy: `field_value_rows` has no `reviewedBy`/`reviewedAt` columns of its own (only `field_values` does), so `corrections.correctedBy`/`correctedAt` is the *only* audit trail a row-level accept has.
- **Stale-extraction exclusion is the single most important correctness property in `queue.ts`.** A document can have multiple `extractions` rows over time (re-running extraction — which happened repeatedly during Phase 3/4 development — inserts a new one each run), and an older, superseded extraction's `field_values` can sit at `status: 'needs_review'` forever if nothing resolves it. The queue resolves each candidate document's true-latest extraction (by `startedAt`) and discards any candidate whose `extractionId` doesn't match, even though the stale row's own `status` column still says `needs_review`. Independently traced against a real two-extraction scenario before commit — correct.
- **A table field and its rows are scored and reviewed independently.** `getNextReviewItem`'s inclusion condition is `field.status = 'needs_review' OR EXISTS a row with status = 'needs_review'` — a table can be `auto_accepted` at the field level (its own reconciliation checks passed) while one specific row still needs a human look, and vice versa isn't assumed either. `acceptField` on a table bulk-resolves any of its own outstanding rows (the whole table is shown when reviewing the field, so accepting it means the reviewer looked at everything on screen); `acceptRow` deliberately never touches the parent `field_values` row.
- **Ordering is `confidence ASC`, a documented v1 simplification.** A table field whose own confidence is high but which has one severely-bad row isn't prioritized by that row's severity — it still surfaces, just not with optimal urgency. Not fixed here; would need a computed sort beyond what this phase's scope justified.
- **Session counters are an atomic SQL increment** (`sql\`column + delta\``), not read-then-write — a double-fired keypress or two tabs against the same session must not silently lose one side's count. A `reviewSessionId` that doesn't exist is validated *before* any field/row mutation and fails the whole request (`404 review_session_not_found`), rather than silently dropping just the count while the review action itself still succeeds.
- **No bbox/highlight overlay on the document image.** `field_values.pageNumber`/`bbox` are never populated by this pipeline (a known gap since Phase 3/4) — `DocViewer` shows the page image plainly rather than fabricating a highlight box from data that doesn't exist.
- **The one global keyboard shortcut**: `Enter`, when nothing is focused (`document.activeElement === document.body`), accepts the current field as-is. This is the actual "keyboard-fast" loop the README describes — glance, Enter, next — without risking a global hotkey hijacking normal typing inside a correction input (every input stops Enter's propagation).

### Testing
- **Backend**: 113 total API tests pass (39 new: `queue.test.ts`, `actions.test.ts`, `review.route.test.ts`), full `npm run typecheck` clean. Built via implement-then-review; the review pass's top priority (stated explicitly, not left implicit) was tracing the stale-extraction scenario against the real code — confirmed correct. One blocking finding fixed before commit: `correctRow` was auditing a legitimately-`null` cell's old value as the literal string `"null"` instead of preserving `null`, corrupting the audit trail. Three more fixed: a missing FK-violation catch on `POST /review-sessions`' `batchId` (now a clean `400 unknown_batch_id` instead of a bare 500, matching `batches.ts`'s existing convention), the read-then-write session-counter race (now atomic), and a bad `reviewSessionId` being silently swallowed instead of failing the request.
- **Frontend**: `npm run typecheck` and `npm run build --workspace web` clean; `oxlint` clean; zero new `web/package.json` dependencies confirmed via diff. Verified with a **real Playwright browser smoke test** (not just static review) — both dev servers actually started (the API boots fine without DB access, since nothing touches the DB until a route is called), and the app was driven through: initial load → reviewer-name gate (confirmed via DOM element counts, not just a screenshot) → name submitted → session-start request genuinely fails against this sandbox's unreachable Postgres (`Connection terminated due to connection timeout`, the same restriction documented since Phase 0) → loading state → error state with a visible Retry button. No blank screen, no uncaught JS error, no crash. Two screenshots sent to the user for the record. One blocking finding fixed before commit: a table row's cell `<input>` was only disabled while a request was `pending`, never while the row itself was already resolved (`row.status !== 'needs_review'`) — since the queue always returns *every* row of a table field for context, not just flagged ones, a reviewer could type into an already-resolved row's cell, hit Enter, and have the edit silently discarded (the server's `400 not_needs_review` is deliberately treated as a no-op success for the *different* case of someone else resolving the item first — this hit that same code path for an unrelated reason, with no visible error). Also fixed: the reviewer-gate's name input didn't stop `Enter` from bubbling to the global "nothing focused" shortcut listener — harmless today only by accident of state ordering (the shortcut's own `queueState` guard can't yet be true at that point), not because the input itself was safe.

### Checkpoint — CLOSED (2026-08-01)
- [x] Pulled and ran on the user's machine (`npm run dev:api` + `npm run dev --workspace web`) — the app loaded for real this time, past the sandbox's forced DB-unreachable error state.
- [x] Reviewer-name gate worked; submitting reached the main layout with the header chrome (name, running counts, "change" control) rendering correctly.
- [x] `GET /review/next` genuinely returned `{item: null}` — the queue is empty. Confirmed real, not broken: the `EmptyState` renders with no interactive elements besides the header's reviewer-"change" link (by design — nothing to act on), which is exactly what the user observed when trying to find something else to click.
- **Why the queue is empty:** every field on every dev-subset document extracted so far landed on `auto_accepted`/already-resolved — consistent with the two documents spot-checked in the Phase 4 checkpoint, both of which came back fully `auto_accepted`.
- **Scope of what this confirms vs. doesn't:** the empty-queue path is confirmed live end-to-end (gate → session → real API call → correctly-rendered empty state). The accept/correct *action* path (clicking Accept, editing a value and saving a correction, a row-level correction) was **not** exercised against a live `needs_review` item in this session — no such item currently exists to click through. That logic is covered by 113 passing backend tests (including a full request/response round-trip via `app.inject()` for every route) and a real Playwright browser smoke test of the surrounding app shell, but not by an actual human click against live data.
- **User's call, explicitly made**: close this checkpoint on the above evidence rather than manufacture a synthetic `needs_review` item (e.g. by lowering a threshold and re-extracting) just to exercise the click path. Recorded here as a known, deliberate gap rather than an oversight — the live accept/correct interaction should get real exercise naturally once a future extraction run (a new document, a harder edge case, or a model that's less confident) actually produces a `needs_review` item.
- Not proceeding to Phase 6 until the user directs it, per the workflow rules.
