# Both optional. Omit both to keep proxying the review UI to a local API
# (npm run dev:api) exactly as before. Copy to .env.local (gitignored) and set
# both to point this dev server at a deployed API instead.
API_PROXY_TARGET=
API_SHARED_SECRET=
